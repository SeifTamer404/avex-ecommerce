import HelpCenter from "@/components/account/HelpCenter";

export const metadata = {
  title: "Help Center | AVEX",
  description: "Find answers to your questions about orders, shipping, returns, and more.",
};

export default function HelpPage() {
  return <HelpCenter />;
}
