export const metadata = {
  title: "Log In",
  description: "Sign in to your AVEX account.",
  alternates: {
    canonical: "/login",
  },
};

export default function LoginLayout({ children }) {
  return <>{children}</>;
}
